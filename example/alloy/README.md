# Alloy example

Copy the `controllers`, `views`, and `styles` directories into an Alloy app that includes `ti.zoomtransition` in `tiapp.xml`.

The `index` controller owns the source table. The `zoomWindow` controller owns and prepares the modal navigation window, its root window, and its scrollable destination table.
